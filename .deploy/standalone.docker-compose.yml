# File has been autogenerated using build-one cli, please do not modify manually.
volumes:
  ah_db_storage: 
  app_db_storage: 
  b1_db_storage: 
  swat_app_uploads: 
  app-data: 
  db-data: 
  n8n-data: 
  redis_storage: 
  auth_db_storage: 
networks:
  app-network:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: 1430
  traefik:
    external: true
services:
  caddy:
    image: docker.cloudsmith.io/buildone/repository/buildone-caddy:24.3.0-b1-399-toast-message-doesnt-work-on-starter.1
    restart: unless-stopped
    networks:
      app-network: 
      traefik: 
    command:
      - caddy
      - run
      - '--config'
      - /etc/caddy/Caddyfile
      - '--adapter'
      - caddyfile
    labels:
      - traefik.enable=true
      - traefik.docker.network=traefik
      - traefik.http.routers.${STACK}-buildone-app.rule=Host(`${STACK}.${DOMAIN}`)
      - traefik.http.routers.${STACK}-buildone-app.entrypoints=${ENTRYPOINTS:-web}
      - traefik.http.services.${STACK}-buildone-app.loadbalancer.server.port=8080
  b1_db_server:
    image: docker.cloudsmith.io/buildone/repository/build-one-postgres-db:17-2
    restart: unless-stopped
    environment:
      - RESET_DATABASES=${RESET_DATABASES:-FALSE}
    healthcheck:
      test:
        - CMD-SHELL
        - /wait_for_db.sh
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      app-network: 
    volumes:
      - b1_db_storage:/var/lib/postgresql/data
  swat_app_server_ts:
    image: docker.cloudsmith.io/buildone/repository/buildone-swat-app-server-ts:24.3.0-b1-399-toast-message-doesnt-work-on-starter.1
    restart: unless-stopped
    environment:
      - APP_URL=${APP_URL:-https://${STACK}.${DOMAIN}}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - IMPORT_DATA=true
      - PORT=3000
      - UPLOAD_DIR=/app-data/uploads
      - B1_DATA_FOLDER=/data/b1
      - APP_DATA_FOLDER=/data/app
      - B1_DATABASE_URL=${B1_DATABASE_URL:-postgres://${B1_DATABASE_CREDENTIALS:-}@b1_db_server:5432/b1?sslmode=disable}
      - AUTH_URL=${AUTH_URL:-http://auth_server:3000}
    networks:
      app-network: 
    volumes:
      - app-data:/app-data
    depends_on:
      b1_db_server:
        condition: service_healthy
  app_db_server:
    image: docker.cloudsmith.io/buildone/repository/build-one-postgres-db:17-2
    restart: unless-stopped
    environment:
      - POSTGRES_DB=app_db
      - RESET_DATABASES=${RESET_DATABASES:-FALSE}
    healthcheck:
      test:
        - CMD-SHELL
        - /wait_for_db.sh
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      app-network: 
    volumes:
      - app_db_storage:/var/lib/postgresql/data
  app_server_ts:
    image: docker.cloudsmith.io/buildone/repository/buildone-app-server-ts:24.3.0-b1-399-toast-message-doesnt-work-on-starter.1
    restart: unless-stopped
    environment:
      - PORT=3000
      - APP_DATABASE_URL=${APP_DATABASE_URL:-postgres://${APP_DATABASE_CREDENTIALS:-}@app_db_server:5432/app_db?sslmode=disable}
      - AUTH_URL=${AUTH_URL:-http://auth_server:3000}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - NODE_ENV=prod
    networks:
      app-network: 
    depends_on:
      app_db_server:
        condition: service_healthy
  n8n_db_server:
    image: docker.cloudsmith.io/buildone/repository/automation-hub-db:1.12.0
    restart: unless-stopped
    healthcheck:
      test:
        - CMD-SHELL
        - /wait_for_db.sh
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      app-network: 
    volumes:
      - ah_db_storage:/var/lib/postgresql/data
  n8n:
    image: docker.cloudsmith.io/buildone/repository/automation-hub-1-121-2-1:1.12.0
    restart: unless-stopped
    environment:
      - SWAT_APP_SERVER_URL=${AUTH_URL:-http://auth_server:3000}
      - AUTHENTICATION_URI=${AUTH_URL:-http://auth_server:3000}
      - DB_POSTGRESDB_HOST=n8n_db_server
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=false
      - N8N_SECURE_COOKIE=false
      - WEBHOOK_URL=https://${STACK}.${DOMAIN}/service/automation
      - EXECUTIONS_MODE=queue
      - QUEUE_BULL_REDIS_HOST=redis
      - QUEUE_HEALTH_CHECK_ACTIVE=true
      - B1_N8N_ENV=prod
    networks:
      app-network: 
    depends_on:
      n8n_db_server:
        condition: service_healthy
      n8n_init_data:
        condition: service_completed_successfully
    volumes:
      - app-data:/workspace/data
      - n8n-data:/workspace/src/data/n8n
  web-app:
    image: docker.cloudsmith.io/buildone/repository/buildone-web-app:24.3.0-b1-399-toast-message-doesnt-work-on-starter.1
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - NODE_ENV=production
      - APP_URL=${APP_URL:-https://${STACK}.${DOMAIN}}
    networks:
      app-network: 
    command:
      - node
      - dist/server/index.mjs
  n8n_init_data:
    image: docker.cloudsmith.io/buildone/repository/buildone-data:24.3.0-b1-399-toast-message-doesnt-work-on-starter.1
    restart: 'no'
    entrypoint:
      - /workspace/node_modules/@buildone/swat-data/.build/initialize_automation_hub_data.sh
    volumes:
      - n8n-data:/update
    networks:
      app-network: 
  redis:
    image: redis:6-alpine
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      app-network: 
    volumes:
      - redis_storage:/data
  ah_worker:
    image: docker.cloudsmith.io/buildone/repository/automation-hub-1-121-2-1:1.12.0
    scale: ${AH_NUM_WORKERS:-1}
    entrypoint:
      - n8n
      - worker
    depends_on:
      - n8n
    environment:
      - EXECUTIONS_MODE=queue
      - QUEUE_BULL_REDIS_HOST=redis
      - QUEUE_HEALTH_CHECK_ACTIVE=true
    restart: unless-stopped
    networks:
      app-network: 
    volumes:
      - app-data:/workspace/data/
  auth_db_server:
    image: docker.cloudsmith.io/buildone/repository/build-one-postgres-db:17-2
    restart: unless-stopped
    environment:
      - POSTGRES_DB=auth_db
      - RESET_DATABASES=${RESET_DATABASES:-FALSE}
    healthcheck:
      test:
        - CMD-SHELL
        - /wait_for_db.sh
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      app-network: 
    volumes:
      - auth_db_storage:/var/lib/postgresql/data
  auth_server:
    image: docker.cloudsmith.io/buildone/repository/buildone-auth-server:24.3.0-b1-399-toast-message-doesnt-work-on-starter.1
    restart: unless-stopped
    environment:
      - PORT=3000
      - APP_URL=${APP_URL:-https://${STACK}.${DOMAIN}}
      - AUTH_DATABASE_URL=${AUTH_DATABASE_URL:-postgres://${AUTH_DATABASE_CREDENTIALS:-}@auth_db_server:5432/auth_db?sslmode=disable}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - B1_AUTOMATION_HUB_USER=${B1_AUTOMATION_HUB_USER}
      - B1_AUTOMATION_HUB_EMAIL=${B1_AUTOMATION_HUB_EMAIL}
      - B1_AUTOMATION_HUB_PASSWORD=${B1_AUTOMATION_HUB_PASSWORD}
      - AH_CREDENTIALS=${AH_CREDENTIALS:-}
      - AUTHENTICATION_TRUSTED_ORIGINS=https://${APP_URL:-${STACK}.${DOMAIN}}
      - MICROSOFT_CLIENT_ID=${MICROSOFT_CLIENT_ID:-}
      - MICROSOFT_CLIENT_SECRET=${MICROSOFT_CLIENT_SECRET:-}
      - MICROSOFT_TENANT_ID=${MICROSOFT_TENANT_ID:-}
      - MICROSOFT_SCOPE=${MICROSOFT_SCOPE:-}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID:-}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET:-}
      - NODE_ENV=production
    networks:
      app-network: 
    depends_on:
      auth_db_server:
        condition: service_healthy
