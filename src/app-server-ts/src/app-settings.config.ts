export default () =>
  ({
    // Define custom application settings here.
    // These values will be available as applicationSettings.custom in the frontend.
    // Example:
    // myApiKey: process.env.MY_API_KEY,
    // featureFlag: process.env.MY_FEATURE === 'true',
  }) as Record<string, unknown>;
