{
  "secrets": [
    {
      "name": "DEFAULT_DATABASE_CREDENTIALS",
      "description": "Default database credentials for all databases (username:password)",
      "note": "Format: username:password. Used for app, b1, and auth databases unless overridden.",
      "canGenerate": true,
      "required": true
    },
    {
      "name": "BETTER_AUTH_SECRET",
      "description": "Secret key for JWT signing and session management (min 32 chars)",
      "note": "Will be auto-generated as a secure 32-character string",
      "canGenerate": true,
      "required": true
    },
    {
      "name": "B1_AUTOMATION_HUB_EMAIL",
      "description": "Email address for Automation Hub (n8n) login",
      "note": "Defaults to 'automation_hub@build.one' if not provided",
      "canGenerate": false,
      "required": false,
      "defaultValue": "automation_hub@build.one"
    },
    {
      "name": "B1_AUTOMATION_HUB_USER",
      "description": "Username for Automation Hub (n8n) authentication",
      "note": "Defaults to 'automation_hub' if not provided",
      "canGenerate": false,
      "required": false,
      "defaultValue": "automation_hub"
    },
    {
      "name": "B1_AUTOMATION_HUB_PASSWORD",
      "description": "Password for Automation Hub (n8n) access",
      "note": "Will be auto-generated if not provided",
      "canGenerate": true,
      "required": false
    },
    {
      "name": "BUILDONE_USER",
      "description": "Build.One platform username",
      "note": "Your Build.One platform username",
      "canGenerate": false,
      "required": true
    },
    {
      "name": "BUILDONE_TOKEN",
      "description": "Build.One platform API token",
      "note": "Obtain from Build.One platform account settings",
      "canGenerate": false,
      "required": true
    }
  ]
}
